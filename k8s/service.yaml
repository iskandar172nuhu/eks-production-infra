apiVersion: v1
kind: Service
metadata:
  name: ia-bestsell-service
  namespace: ia-bestsell
spec:
  selector:
    app: ia-bestsell-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: LoadBalancer